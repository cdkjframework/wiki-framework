spring:
  application:
    name: cdkjframework-config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/langzhiyun/cloud-config-repository.git #配置文件所在仓库
          username: lesmarthome
          password: TgFP24h%&hADbc$c
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录